load("@rules_python//python:defs.bzl", "py_binary", "py_library")
load("@rules_python//python:defs.bzl", "py_binary", "py_library")

py_library(
    name = "models",
    srcs = glob(["**/*.py"]),
    data = [
        "//pythonp/apps/tokenfate/static:assets",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "@pypi_deps//:flask",
        "@pypi_deps//:flask_sqlalchemy",
        "@pypi_deps//:sqlalchemy",
    ],
)
