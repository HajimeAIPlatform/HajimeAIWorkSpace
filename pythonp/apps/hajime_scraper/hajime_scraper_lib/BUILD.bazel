load("@rules_python//python:defs.bzl", "py_library")

# define a library for the scraper
py_library(
    name = "hajime_scraper_lib",
    srcs = glob(["**/*.py"]),
    visibility = ["//visibility:public"],
    deps = [
         "@pypi_deps//:scrapy",
         "@pypi_deps//:requests",
    ],
)
